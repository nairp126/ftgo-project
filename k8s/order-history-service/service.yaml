apiVersion: v1
kind: Service
metadata:
  name: order-history-service

spec:
  selector:
    app: order-history-service

  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP

  type: ClusterIP