apiVersion: v1
kind: Service

metadata:
  name: ftgo-order-service
  namespace: ftgo
  labels:
    app: ftgo-order-service
    component: order

spec:
  selector:
    app: ftgo-order-service

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP
