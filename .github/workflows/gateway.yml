name: Gateway CI/CD Pipeline

on:
  push:
    branches: [main, dev]
    paths:
      - 'Universal-AI-Gateway/**'
      - 'k8s/gateway/**'
      - '.github/workflows/gateway.yml'
  pull_request:
    branches: [main, dev]
    paths:
      - 'Universal-AI-Gateway/**'
      - 'k8s/gateway/**'
      - '.github/workflows/gateway.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ secrets.AWS_REGION || vars.AWS_REGION || 'ap-south-1' }}
  ECR_REPOSITORY: universal-ai-gateway
  EKS_CLUSTER_NAME: ${{ secrets.EKS_CLUSTER_NAME || vars.EKS_CLUSTER_NAME || 'ftgo-cluster' }}

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r Universal-AI-Gateway/requirements-dev.txt pytest-mock

      - name: Run Tests
        run: |
          cd Universal-AI-Gateway
          pytest tests/ -v --cov=app

  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION || vars.AWS_REGION || 'ap-south-1' }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2
        id: ecr-login

      - name: Build and Push Docker Image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./Universal-AI-Gateway
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  deploy:
    name: Deploy to EKS
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION || vars.AWS_REGION || 'ap-south-1' }}

      - name: Update kubeconfig
        continue-on-error: true
        run: |
          aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.EKS_CLUSTER_NAME }} || true

      - name: Apply Base Configs
        continue-on-error: true
        run: |
          kubectl apply -f k8s/gateway/configmap.yaml -n ftgo
          kubectl apply -f k8s/gateway/service.yaml -n ftgo
          kubectl apply -f k8s/gateway/ingress.yaml -n ftgo

      - name: Deploy Gateway with Substituted Image
        continue-on-error: true
        run: |
          sed -i "s|\${ECR_REGISTRY}|${{ steps.login-ecr.outputs.registry }}|g" k8s/gateway/deployment.yaml
          sed -i "s|\${IMAGE_TAG}|${{ github.sha }}|g" k8s/gateway/deployment.yaml
          kubectl apply -f k8s/gateway/deployment.yaml -n ftgo

      - name: Wait for Rollout
        continue-on-error: true
        run: |
          kubectl rollout status deployment/universal-ai-gateway -n ftgo --timeout=300s
