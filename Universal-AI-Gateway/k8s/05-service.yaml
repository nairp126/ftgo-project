apiVersion: v1
kind: Service
metadata:
  name: llm-gateway-svc
  namespace: universal-llm-gateway
  labels:
    app: llm-gateway
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-gateway
