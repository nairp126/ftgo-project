spring.application.name=ftgo-order-history-service

# ==========================
# Server
# ==========================
server.port=8082

# ==========================
# Database
# ==========================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/order_history_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:anshumanrangarh}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================
# JPA / Hibernate
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ==========================
# Logging
# ==========================
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# ==========================
# Kafka Configuration
# ==========================
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Consumer
spring.kafka.consumer.group-id=order-history-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# Producer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# ==========================
# FTGO Kafka Topics
# ==========================
ftgo.kafka.topics.order-created=order.created
ftgo.kafka.topics.payment-authorized=payment.authorized
ftgo.kafka.topics.payment-failed=payment.failed
ftgo.kafka.topics.ticket-created=ticket.created
ftgo.kafka.topics.ticket-rejected=ticket.rejected